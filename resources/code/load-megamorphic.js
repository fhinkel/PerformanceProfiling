function load(obj) {
  return obj.x;
}

var obj = {};
load(obj);

obj.a = 0;
load(obj);

obj.b = 0;
load(obj);

obj.c = 0;
load(obj);

obj.d = 0;
load(obj);

obj.e = 0; // 5 different shapes
load(obj);
